# Contexto Técnico del Proyecto: Gestión-Alumnos (Haspenrg)

Este documento es el eje central de sincronización del ecosistema de desarrollo para el **Ecosistema de Gestión Escolar del Colegio HASPEN**. Es obligatorio leer este archivo al iniciar cualquier interacción técnica y mantenerlo actualizado ante cada hito resuelto.

---

## 📑 PARTE 1: Descripción del Proyecto, Archivos e Infraestructura

### Contexto General
El propósito de este ecosistema es coordinar la administración académica, matriculación, calificaciones y reportes analíticos de los alumnos. El desarrollo se rige por un estricto control de estado para garantizar que toda modificación mantenga la coherencia lógica del sistema.

### Inventario de Archivos del Proyecto
El sistema se compone de los siguientes archivos ordenados por lenguaje de programación:

*   **Páginas Web (HTML5 & Tailwind CSS):**
    *   `index.html` — Portal de login y acceso inicial.
    *   `panel.html` — Panel principal de navegación general.
    *   `usuarios.html` — Interfaz para administración de cuentas de usuarios.
    *   `inscripcion.html` — Interfaz de legajos digitales y matrículas.
    *   `cursos-materias.html` — Configuración de oferta académica.
    *   `roles.html` — Control de perfiles y accesos.
    *   `calificaciones.html` — Carga de notas por materias.
    *   `boletin.html` — Visualización de reportes analíticos.
    *   `estadisticas.html` — Panel visual de rendimiento.
*   **Lógica del Negocio (JavaScript - ES6 & Firebase v10):**
    *   `app.js` — Control base y enrutamiento inicial.
    *   `firebase-config.js` — Inicialización centralizada de Firebase.
    *   `usuarios.js` — Lógica de autenticación y CRUD de personal.
    *   `inscripcion.js` — Manejo del ciclo de vida del alumno y vistas.
    *   `carga-masiva.js` — Importación de datos por planillas CSV.
    *   `roles.js` — Middleware y permisos en el cliente.
    *   `calificaciones.js` — Procesamiento y guardado de notas.
    *   `boletin.js` — Cálculos de promedios trimestrales.
    *   `estadisticas.js` — Gráficos y métricas de deserción.
*   **Estilos y Medios:**
    *   `estilos.css` — Estilos complementarios a Tailwind.
    *   `logo.jpg`, `logo1.png`, `logo1a.png`, `IMG-20260519-WA0020.jpg` — Activos de identidad institucional.

### 🚨 Reglas Obligatorias de Trabajo (Mandatorias)
1.  **Diagnóstico Técnico Previo:** Es obligatorio desglosar los componentes afectados, las relaciones funcionales y la estrategia de solución antes de proponer cualquier bloque de código mediante un análisis conciso de arquitectura.
2.  **Razonamiento Lógico y Cero Suposiciones:** Queda estrictamente prohibido adivinar. El código se generará única y exclusivamente de acuerdo al análisis detallado de las codificaciones realizadas y siguiendo un razonamiento deductivo formal y riguroso.
3.  **Prohibición Total de Archivos Largos:** Queda terminantemente prohibido enviar archivos completos o módulos que excedan las 120 líneas de código. Se deben proveer únicamente funciones específicas, métodos aislados o bloques atómicos de lógica.
4.  **Mecanismo de Inyección por Parches:** Los fragmentos de código deben indicarse con referencias claras de ubicación, comentarios de cabecera que sirvan de ancla de búsqueda rápida (`Ctrl + F`) y líneas exactas de reemplazo para evitar la destrucción del entorno existente.
5.  **Preservación Estructural:** Queda prohibido alterar o sobreescribir los IDs, nombres de clases preexistentes, variables de entorno global del cliente o las maquetaciones estéticas con Tailwind CSS/CSS nativo responsivo del layout.
6.  **Evasión de Bloqueos por URLs (Fragmentación CDN):** El sistema posee restricciones estrictas para inyectar enlaces web y URLs de librerías CDN de Firebase directamente por código. Se debe utilizar **obligatoriamente** la técnica de fragmentación y concatenación dinámica de caracteres implementada en la arquitectura del proyecto:
    ```javascript
    const bAuth = 'h' + 't' + 't' + 'p' + 's' + ':' + '/' + '/' + 'w' + 'w' + 'w' + '.' + 'g' + 's' + 't' + 'a' + 't' + 'i' + 'c' + '.' + 'c' + 'o' + 'm' + '/f' + 'i' + 'r' + 'e' + 'b' + 'a' + 's' + 'e' + 'j' + 's' + '/10.12.0/';
    const { createUserWithEmailAndPassword } = await import(bAuth + 'firebase-auth.js');
    ```

---

## 📊 PARTE 2: Módulos Realizados y Funcionando Sin Errores

### 1. Gestión de Usuarios (`usuarios.html` / `usuarios.js`)
*   **Roles y Cursos:** Centralización e interfaces responsivas para el alta de perfiles administrativos y de maestría.
*   **Edición Indexada:** Se corrigió la función `window.activarModoEdicion`. Se reemplazó la asignación de arreglos planos por una desestructuración indexada individual en los elementos selectores `<select id="altaAnio1">` y `altaAnio2`.
*   **Persistencia Asíncrona:** Se adaptó la función `inicializarSelectoresCursos` a la sintaxis modular oficial de Firebase v10 (`collection` y `getDocs`), asegurando que si los cursos escolares no se encuentran en el `localStorage`, el sistema realice un fetch directo y limpie los textos estáticos en producción.
*   **Credenciales Sincronizadas:** Se corrigió el método `procesarGuardarUsuario`. Se inyectaron capturas válidas de los inputs de contraseña (`claveUsuario` y `confirmarClaveUsuario`), impidiendo el envío de strings vacíos a Firebase Auth. Ahora las altas de nuevos usuarios impactan de forma sincronizada tanto en el motor de autenticación como en Firestore.

### 2. Legajo Digital e Inscripción (`inscripcion.html` / `inscripcion.js`)
*   **Identificadores:** Generación automatizada de CUIL y persistencia indexada de legajos familiares de tutores.
*   **Permisos de Lectura:** Se reestructuró la lógica de renderizado de la matrícula. Se transformó la condicional restrictiva de roles fijos a un `else` generalizado de solo lectura. Ahora, los usuarios con privilegios de solo lectura (como preceptores o auditores) disponen de los botones de **Informe** y **Boletín**, exceptuando el tacho institucional de eliminación física.
*   **Modales Reactivos:** Se solucionó la destrucción de escuchadores en el ciclo de vida del DOM inyectando una Delegación de Eventos sobre el elemento contenedor `#modalImpresionContenedor`. El botón "Cerrar" responde de forma instantánea sin importar cuántas veces se regenere el HTML interno de las previsualizaciones.
*   **Ficha de Emergencia Desacoplada (Data-Attributes):** Se implementó un botón exclusivo para perfiles de Solo Lectura llamado **"👁 Datos"**. Los domicilios, teléfonos de contacto destacados y datos de adultos responsables se inyectan en tiempo de renderizado dentro de atributos `data-` en el mismo nodo HTML del botón. Esto elimina las búsquedas y dependencias de arreglos volátiles en memoria JavaScript (`listaAlumnosGlobal`), garantizando la consulta veloz ante contingencias. El modo escritura (Administrador) no renderiza este botón para evitar redundancias con su panel lateral.

---

## ⚠️ PARTE 3: Módulos Pendientes, a Medias o con Errores

### Inconvenientes Críticos Activos (Prioridad Uno)
*   **Bloqueo de Visibilidad en Carga Masiva:** Se integró el modal flotante de simulación y la barra de herramientas. Sin embargo, en el último despliegue en producción, el contenedor de la interfaz (`#contenedorCargaMasiva`) dejó de visualizarse para el rol Administrador.
    *   *Causa Raíz a Diagnosticar:* Un aparente bloqueo silencioso de inicialización o un conflicto asíncrono con el flujo principal de `inscripcion.js`, que impide al script de carga masiva remover la propiedad de ocultado CSS.
*   **Planilla CSV 2024:** Se requiere rastrear de forma aislada el ciclo de renderizado del botón y normalizar el buscador de bloques de cursos para la planilla CSV del Ciclo Lectivo 2024.

### Módulos por Implementar o Diseñar
*   **Módulo de Calificaciones (`calificaciones.html`):** Auditoría y diseño de flujos para carga de notas trimestrales.
*   **Módulo de Boletines (`boletin.html`):** Automatización y vistas de promedios para reportes analíticos.
*   **Módulo de Estadísticas (`estadisticas.html`):** Tableros visuales de rendimiento y deserción escolar.
