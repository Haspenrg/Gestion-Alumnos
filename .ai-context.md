# Contexto Técnico del Proyecto: Gestión-Alumnos (Haspenrg)

Este documento es el eje central de sincronización del ecosistema de desarrollo para el **Ecosistema de Gestión Escolar del Colegio HASPEN**. Es obligatorio leer este archivo al iniciar cualquier interacción técnica y mantenerlo actualizado ante cada hito resuelto.
---
## 📑 PARTE 1: Descripción del Proyecto, Archivos e Infraestructura

### Contexto General
El propósito de este sistema es coordinar la administración académica, matriculación, calificaciones y reportes analíticos de los alumnos. El desarrollo se rige por un estricto control de estado para garantizar que toda modificación mantenga la coherencia lógica del sistema.

### Inventario de Archivos del Proyecto
El sistema se compone de los siguientes archivos ordenados por lenguaje de programación:

*   **Páginas Web (HTML5 & Tailwind CSS):**
    *   `index.html` — Portal de login y acceso inicial.
    *   `panel.html` — Panel principal de navegación general.
    *   `usuarios.html` — Interfaz para administración de cuentas de usuarios.
    *   `inscripcion.html` — Interfaz de legajos digitales y matrículas.
    *   `cursos-materias.html` — Configuración de oferta académica.
    *   `roles.html` — Control de perfiles y accesos.
    *   `calificaciones.html` — Carga de notas por materias.
    *   `boletin.html` — Visualización de reportes analíticos.
    *   `estadisticas.html` — Panel visual de rendimiento.
*   **Lógica del Negocio (JavaScript - ES6 & Firebase v10):**
    *   `app.js` — Control base y enrutamiento inicial.
    *   `firebase-config.js` — Inicialización centralizada de Firebase.
    *   `usuarios.js` — Lógica de autenticación y CRUD de personal.
    *   `inscripcion.js` — Manejo del ciclo de vida del alumno y vistas.
    *   `carga-masiva.js` — Importación de datos por planillas CSV.
    *   `roles.js` — Middleware y permisos en el cliente.
    *   `calificaciones.js` — Procesamiento y guardado de notas.
    *   `boletin.js` — Cálculos de promedios trimestrales.
    *   `estadisticas.js` — Gráficos y métricas de deserción.
*   **Estilos y Medios:**
    *   `estilos.css` — Estilos complementarios a Tailwind.
    *   `logo.jpg`, `logo1.png`, `logo1a.png`, `IMG-20260519-WA0020.jpg` — Activos de identidad institucional.

### 🚨 Reglas Obligatorias de Trabajo (Mandatorias)
1.  **Diagnóstico Técnico Previo:** Es obligatorio desglosar los componentes afectados, las relaciones funcionales y la estrategia de solución antes de proponer cualquier bloque de código mediante un análisis conciso de arquitectura.
2.  **Razonamiento Lógico y Cero Suposiciones:** Queda estrictamente prohibido adivinar o suponer. El código se generará única y exclusivamente de acuerdo al análisis detallado de las codificaciones realizadas y siguiendo un razonamiento deductivo formal y riguroso.
3.  **Prohibición Total de Archivos Largos:** Queda terminantemente prohibido enviar archivos completos o módulos que excedan las 150 líneas de código a no ser que sea dividido en partes. Se deben proveer únicamente funciones específicas, métodos aislados o bloques de lógica.
4.  **Mecanismo de Inyección por Parches:** Los fragmentos de código deben indicarse con referencias claras de ubicación, Con pocos comentarios de cabecera. La busqueda se realizara a traves de (`Ctrl + F`) y líneas exactas de reemplazo para evitar la destrucción del entorno existente.
5.  **Preservación Estructural:** Queda prohibido alterar o sobreescribir los IDs, nombres de clases preexistentes, variables de entorno global del cliente o las maquetaciones estéticas con Tailwind CSS/CSS nativo responsivo del layout.
6.  **Evasión de Bloqueos por URLs (Fragmentación CDN):** El sistema posee restricciones estrictas para inyectar enlaces web y URLs de librerías CDN de Firebase directamente por código. Se debe utilizar **obligatoriamente** la técnica de fragmentación y concatenación dinámica de caracteres implementada en la arquitectura del proyecto:
    ```javascript
    const cdn = 'h' + 't' + 't' + 'p' + 's' + ':' + '/' + '/' + 'w' + 'w' + 'w' + '.' + 'g' + 's' + 't' + 'a' + 't' + 'i' + 'c' + '.' + 'c' + 'o' + 'm' + '/f' + 'i' + 'r' + 'e' + 'b' + 'a' + 's' + 'e' + 'j' + 's' + '/10.12.0/';
    ```

---

## 📊 PARTE 2: Módulos Realizados y Funcionando Sin Errores

### 1. Gestión de Usuarios (`usuarios.html` / `usuarios.js`)
*   **Roles y Cursos:** Centralización e interfaces responsivas para el alta de perfiles administrativos y de maestría.
*   **Edición Indexada:** Se reemplazó la asignación de arreglos planos por una desestructuración indexada individual en los elementos selectores `<select id="altaAnio1">` y `altaAnio2`.
*   **Persistencia Asíncrona:** Adaptación a la sintaxis modular oficial de Firebase v10 (`collection` y `getDocs`), asegurando fetch directo si falla el `localStorage`.
*   **Credenciales Sincronizadas:** Validación estricta en el método `procesarGuardarUsuario` impidiendo contraseñas vacías en Firebase Auth y Firestore.

### 2. Legajo Digital e Inscripción (`inscripcion.html` / `inscripcion.js`)
*   **Identificadores:** Generación automatizada de CUIL y persistencia indexada de legajos familiares de tutores.
*   **Permisos de Lectura:** Renderizado condicional restrictivo mediante un `else` generalizado de solo lectura (preceptores y auditores ven botones sin acceso a borrado físico).
*   **Modales Reactivos:** Delegación de Eventos sobre el elemento contenedor `#modalImpresionContenedor` asegurando el cierre instantáneo del modal.
*   **Ficha de Emergencia Desacoplada (Data-Attributes):** Botón exclusivo de solo lectura **"👁 Datos"** que extrae información mediante atributos `data-` sin depender de arreglos volátiles en memoria (`listaAlumnosGlobal`).
*   **Acoplamiento de Carga Masiva:** Inyección de la llamada global segura `window.inicializarCargaMasivaSegura()` al finalizar el ciclo de `inicializarModuloInscripciones` para mitigar bloqueos de asincronía.

---
### 3. Configuración de Oferta Académica ( cursos-materias.html )
- **Scroll de Contención Vertical:** Implementación de un alto máximo rígido de 500px (`max-height: 500px; overflow-y: auto`) en la columna derecha para evitar el estiramiento indefinido del layout general.
- **Caché Reactiva en Memoria:** Migración del renderizado directo de Firestore a un sistema de persistencia indexada local (`let cacheCursos = []`) para mitigar el consumo excesivo de cuotas de lectura de base de datos.
- **Filtros Multidimensionales:** Diseño e integración de una barra superior con selectores reactivos cruzados para segmentación en caliente por Año/Ciclo y Orientación Académica.


## ⚠️ PARTE 3: Módulos Pendientes, a Medias o con Errores

### Módulos Realizados a Medias (Fase de Pruebas / No Completado 100%)
*   **Módulo de Carga Masiva (`carga-masiva.js`):** El motor fue reestructurado con éxito para solucionar de raíz el error crítico de visibilidad del contenedor `#contenedorCargaMasiva` mediante una función de arranque expuesta globalmente y acoplada a `inscripcion.js`. Se optimizó el flujo de red reemplazando lecturas unitarias por una precarga indexada en un `Set`. Se implementó un mapeador dinámico por expresiones regulares avanzadas (`RegEx`) para ignorar encabezados del título de escuela y admitir nombres con comas internas.
 **Módulo de Carga Masiva ( carga-masiva.js ):** Operativo en fase de simulación de datos en memoria local. Resta validar en producción los flujos de escritura final en Firestore y el refresco reactivo de la nómina del legajo.
    *   *Estado actual:* No esta totalmente operativo no se visualiza el modal previo a la insercion en la base de datos. Resta validar en producción los flujos de escritura final en Firestore y el refresco reactivo de la nómina del legajo al impactar planillas extensas.

### Módulos por Implementar o Diseñar
*   **Módulo de Calificaciones (`calificaciones.html`):** Auditoría y diseño de flujos para carga de notas trimestrales.
*   **Módulo de Boletines (`boletin.html`):** Automatización y vistas de promedios para reportes analíticos.
*   **Módulo de Estadísticas (`estadisticas.html`):** Tableros visuales de rendimiento y deserción escolar.

- **Acoplamiento de Carga Masiva:** Inyección de la llamada global window.inicializarCargaMasivaSegura() al finalizar el ciclo de inicializarModuloInscripciones.


## 📊 PARTE 3: Módulos Pendientes, a Medias o con Errores

### Módulos Realizados a Medias (Fase de Pruebas / Con Errores)
-
<!-- ==========================================================================
     BITÁCORA DE AVANCES Y REFACTORIZACIONES (SESIÓN: 27/05/2026)
     ========================================================================== -->

## 📈 1. Avances de UI/UX Consolidados (Inscripción)
Se incrementó con éxito la densidad de información en `inscripcion.html` y `inscripcion.js` bajo los siguientes parámetros técnicos:
*   **Compactación del Formulario**: Se redujeron los padding e interlineados de los campos críticos de texto (Inputs, Selects, Textareas) de 38px a 30px, normalizando por CSS local con `!important` el selector de Género (`#generoAlumno`) para neutralizar clases utilitarias de Tailwind que estiraban el formulario.
*   **Sección de Documentación**: Se compactaron los márgenes del bloque gris `.grupo-documentacion` y `.linea-documento-digital`, reduciendo el tamaño físico de los botones de acción (📁 y 👁) a 26px para evitar scrolls verticales innecesarios en pantallas de escritorio.
*   **Paginación Lógica de Alumnos**: Se implementó una segmentación matemática de 25 registros en caliente (`Client-Side Pagination`) dentro de la función `renderizarFilasEnTabla(alumnos)` en `inscripcion.js`, utilizando el método nativo `.slice()` para encuadrar rígidamente el listado vertical.
*   **Sincronización de Filtros**: Se blindó la función asíncrona `procesarFiltrosYNomina()` forzando un reset de estado (`window.paginaActualAlumnos = 1`) al inicio de cada evento de búsqueda o filtrado por curso, previniendo desborda mientos de arreglos o pantallas vacías.

## ⚠ 2. Frentes Pendientes y Bloqueos Identificados (Próxima Sesión)
Al concluir la sesión, se detectó un comportamiento anómalo en el renderizado final de la barra de navegación de páginas en el navegador que requiere una auditoría estructural profunda:
*   **Conflicto de Cascades / Duplicación del DOM**: La botonera inferior (`btnPrevDesktop`, `btnNextDesktop`, `btnResetPagina`) se renderiza en la pantalla con el aspecto plano y gris nativo de HTML5, ignorando las propiedades utilitarias de Tailwind y los estilos inline.
*   **Síntoma de Diagnóstico**: El botón rojo global "Volver al Panel" aparece duplicado en la interfaz activa (una vez abajo a la izquierda del formulario y otra en el centro debajo del listado). Esto denota que existe un script o rutina asíncrona en el cliente que está reescribiendo o inyectando fragmentos del DOM de forma cíclica o desordenada, pisando los estilos de los controles inyectados.
# Contexto Técnico del Proyecto: Gestión-Alumnos (Haspenrg)

Este documento es el eje central de sincronización del ecosistema de desarrollo para el **Ecosistema de Gestión Escolar del Colegio HASPEN**. Es obligatorio leer este archivo al iniciar cualquier interacción técnica y mantenerlo actualizado ante cada hito resuelto.

## 📑 PARTE 1: Descripción del Proyecto, Archivos e Infraestructura

#

## 📊 PARTE 2: Módulos Realizados y Funcionando Sin Errores

### 1. Cabecera Estética y Jerárquica del Panel Principal ( panel.html )
- **Estructura Reorganizada:** Distribución horizontal simétrica mediante Grid de tres columnas limpias, centrando de forma imponente la identidad escolar.
- **Jerarquía Visual:** Título central configurado a un tamaño de `2.1rem`, peso tipográfico `900` (black) en mayúsculas sostenidas, con interlineado corregido a `line-height: 1;` bajo el color azul institucional `#1b4d82`. Subtítulo del profesor "Prof. Luis Felippa Adam" estilizado en cursiva (`font-style: italic`) con tamaño `1.05rem`.
- **Agrandado de Escudo:** Eliminación de restricciones previas heredadas. La imagen `logo1.png` se rediseñó mediante estilos inline fijos a `height: 110px !important; width: auto !important; object-fit: contain;` para garantizar nitidez y escala institucional en la esquina derecha.
- **Preservación Funcional:** Los identificadores asíncronos `id="saludo-usuario"` e `id="rol-usuario"` se mantuvieron intactos, garantizando que los scripts de autenticación sigan inyectando los datos de personal dinámicamente.

### 2. Borde Institucional en Cuadrícula de Módulos ( estilos.css )
- **Estilo de Tarjetas:** Se modificó la regla `.modulo-card` (líneas 211 a 218) para sustituir el borde gris tenue por una fina línea definida en azul institucional utilizando la propiedad `border: 1px solid #1b4d82;`, unificando la estética general del panel.

### 3. Gestión de Usuarios ( usuarios.html / usuarios.js )
- **Roles y Cursos:** Centralización e interfaces responsivas para el alta de perfiles administrativos y de maestría.
- **Edición Indexada:** Se reemplazó la asignación de arreglos planos por una desestructuración indexada individual en los elementos selectores `<select id="altaAnio1">` y `altaAnio2`.
- **Persistencia Asíncrona:** Adaptación a la sintaxis modular oficial de Firebase v10 (`collection` y `getDocs`), asegurando fetch directo si falla el localStorage.
- **Credenciales Sincronizadas:** Validación estricta en el método `procesarGuardarUsuario` impidiendo contraseñas vacías en Firebase Auth y Firestore.

### 4. Legajo Digital e Inscripción ( inscripcion.html / inscripcion.js )
- **Identificadores:** Generación automatizada de CUIL y persistencia indexada de legajos familiares de tutores.
- **Permisos de Lectura:** Renderizado condicional restrictivo mediante un else generalizado de solo lectura (preceptores y auditores ven botones sin acceso a borrado físico).
- **Modales Reactivos:** Delegación de Eventos sobre el elemento contenedor `#modalImpresionContenedor` asegurando el cierre instantáneo del modal.
- **Ficha de Emergencia Desacoplada (Data-Attributes):** Botón exclusivo de solo lectura **"👁 Datos"** que extrae información mediante atributos data- sin depender de arreglos volátiles en memoria (`listaAlumnosGlobal`).
- **Acoplamiento de Carga Masiva:** Inyección de la llamada global segura `window.inicializarCargaMasivaSegura()` al finalizar el ciclo de `inicializarModuloInscripciones` para mitigar bloqueos de asincronía.

### 5. Configuración de Oferta Académica ( cursos-materias.html )
- **Scroll de Contención Vertical:** Implementación de un alto máximo rígido de 500px (`max-height: 500px; overflow-y: auto`) en la columna derecha para evitar el estiramiento indefinido del layout general.
- **Caché Reactiva en Memoria:** Migración del renderizado directo de Firestore a un sistema de persistencia indexada local (`let cacheCursos = []`) para mitigar el consumo excesivo de cuotas de lectura de base de datos.
- **Filtros Multidimensionales:** Diseño e integración de una barra superior con selectores reactivos cruzados para segmentación en caliente por Año/Ciclo y Orientación Académica.

### 6. Avances de UI/UX Consolidados (Inscripción)
- **Compactación del Formulario:** Se redujeron los padding e interlineados de los campos críticos de texto (Inputs, Selects, Textareas) de 38px a 30px, normalizando por CSS local con `!important` el selector de Género (`#generoAlumno`) para neutralizar clases de Tailwind.
- **Sección de Documentación:** Se compactaron los márgenes del bloque gris `.grupo-documentacion` y `.linea-documento-digital`, reduciendo el tamaño físico de los botones de acción a 26px.
- **Paginación Lógica de Alumnos:** Se implementó una segmentación matemática de 25 registros en caliente (Client-Side Pagination) dentro de la función `renderizarFilasEnTabla(alumnos)` en `inscripcion.js`, utilizando el método nativo `.slice()`.
- **Sincronización de Filtros:** Se blindó la función asíncrona `procesarFiltrosYNomina()` forzando un reset de estado (`window.paginaActualAlumnos = 1`) al inicio de cada evento de búsqueda o filtrado por curso.

---

## ⚠ PARTE 3: Módulos Pendientes, a Medias o con Errores

### Módulos Realizados a Medias (En fase de desarrollo / Con Errores pendientes)

- **Módulo de Carga Masiva ( carga-masiva.js ):** El motor procesa planillas CSV continuas de gran tamaño que contienen la información correlativa de 1° a 6° año de todo el Turno Mañana. Se eliminó la interrupción temprana (`break`) permitiendo continuar la búsqueda a lo largo de todo el documento.
    - *Logro UI/UX Parcial:* El sistema ya aísla de forma impecable el listado del curso **1° Año "E"** reflejando correctamente su nómina en la ventana de simulación flotante.
    - *El Bloqueo Crítico Técnico (Falsos Positivos Ocultos):* Al intentar cambiar y procesar el curso **2° Año "A"** (o divisiones superiores), la lógica de texto plano actual con `.includes()` colisiona de forma silenciosa. Se descubrieron mediante auditoría profunda en la consola del navegador dos interferencias gramaticales exactas:

<

### 2. Conexión en Módulo de Calificaciones y Boletines
* **Objetivo:** Capturar de forma forense cada alta, modificación o rectificación de notas efectuada por docentes o preceptores para resguardar el historial de rendimiento.
* **Evento semántico:** `categoria: "CALIFICACIONES"`, `subcategoria: "CARGA_NOTA" | "RECTIFICACION"`.

### 3. Interfaz Visual de Trayectoria (Línea de Tiempo)
* **Objetivo:** Diseñar un componente visual nativo (Timeline cronológico) dentro de la interfaz para consultar la historia del alumno de 1° a 6° año.
* **Restricción Crítica de Costos:** Se debe implementar de forma perezosa (Lazy Loading) mediante lectura bajo demanda gatillada exclusivamente por interacción del usuario (ej. botón "👁 Datos" o similar), blindando la cuota diaria gratuita (Spark Plan) de 50,000 lecturas en Cloud Firestore.
## 📌 Estado de la Sesión Actual (29/05/2026) - Módulo de Trazabilidad e Historial

### 🔹 Lo que quedó 100% Resuelto y Estable:
1. **Automatización de CUIL (Tutor)**: Corregido en `inscripcion.js`. Se inyectó la fórmula matemática estándar de ANSES de forma síncrona en el formulario manual. Ahora, al ingresar el DNI del adulto responsable y alternar su género, el CUIL se autocompleta instantáneamente en pantalla.
2. **Formulario de Edición**: Solucionada la duplicación silenciosa de nombres provenientes de la carga masiva en `inscripcion.js`. Al hacer clic en editar, el nombre se limpia automáticamente por la mitad si viene repetido de Firestore, y se sincronizan correctamente los selectores de género de alumnos y tutores.
3. **Motor de Deducción de Género**: Corregido el parsing semántico en `carga-masiva.js`. Se reestructuró la lógica para evaluar la matriz de palabras completas, evitando que el orden de los apellidos en el Excel altere la deducción (ej. *Micaela Nair* ya se registra correctamente como Femenino con prefijo 27).
4. **Saneamiento Sintáctico**: Reparados los tres errores de compilación (`EOF` y llaves huérfanas) al final de `carga-masiva.js`. El archivo quedó con cero alertas en VS Code.
5. **Firma Forense en Tiempo Real**: Se dinamizó `historial.js` para extraer el operador activo de la sesión (`localStorage`) y se expuso la función en el ámbito global (`window.registrarEventoLegajo`). Se conectaron los disparadores de auditoría en `inscripcion.js` (altas, modificaciones y bajas manuales) y en `carga-masiva.js` (procesamiento por lotes de Excel).
6. **Estructura del Historial**: Se crearon los archivos independientes `historial.html` (interfaz base) e `historial-view.js` (controlador de lectura) para evitar sobrecargar el flujo de matrícula. En `inscripcion.js` se inyectó dinámicamente el botón "📜 Historial" en la grilla de alumnos para redirigir pasando el parámetro DNI en la URL (`historial.html?dni=...`).

### 🛑 Puntos Críticos Pendientes para la Próxima Sesión:
-
- **Fallo de Navegación en el Historial (`historial.html`)**: El botón inferior diseñado para regresar a la pantalla principal ("Volver al Panel") no responde o no efectúa la redirección esperada para el operador.
- **Detalle Forense Insuficiente**: Las descripciones de las modificaciones generales del legajo necesitan registrar el rastro específico del movimiento (ej: cuando un alumno cambia de curso, registrar de qué sección viene y a cuál va, o cuál fue el motivo explícito de una baja definitiva).
- **Conexión de Futuros Módulos**: El backend del historial ya está listo para recibir eventos; queda pendiente diseñar las firmas inmutables cuando se desarrollen las pantallas de **Calificaciones Cuatrimestrales**, **Boletines** y **Estadísticas**.

El desarrollo del módulo "Previas" del Colegio HASPEN ha finalizado con la limpieza de previas.html y previas.js, saneando duplicaciones y errores de sintaxis. Se acordó migrar la lectura de datos de una subcolección a una colección raíz independiente (previas) para optimizar el uso del Plan Spark de Firebase. El próximo paso consiste en actualizar la función cargarPlanillaGeneralAlArrancar para implementar esta nueva arquitectura, migrar los datos históricos y renderizar la planilla.
